
BY DEFALUT all containers in docker compose  are in  same network , like this

             default
 ┌─────────────────────────────┐
 │ vote redis worker db result │
 └─────────────────────────────┘







exemple but architecture  :

front-end
   │
 vote ───── result
   │          │
   └────┬─────┘
        │
back-end
   │
 redis ─ worker ─ db



then  , the docker compose.yml  will add network   ( vote - result  / vote - back  / result - redis  /  vote - redis  /   redis - worker - db  )

networks  : frontend  / backend   ,  houma  eli bch yamlou  liaison






services:

  vote:
    image: voting-app
    ports:
      - "5000:80"
    networks:
      - front-end
      - back-end

  result:
    image: result
    ports:
      - "5001:80"
    networks:
      - front-end
      - back-end

  redis:
    image: redis
    networks:
      - back-end

  db:
    image: postgres:9.4
    networks:
      - back-end

  worker:
    image: worker
    networks:
      - back-end


networks:
  front-end:
  back-end:







